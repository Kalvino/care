import { Component } from '@angular/core';

/**
 * registration layout component
 * allows separate styling for the registration module
 */
@Component({
  selector: 'care-register-layout',
  template: `<care-register-page></care-register-page>`
})
export class RegisterLayoutComponent {
}
