/**
 * definition of registration credentials
 * TODO: needs more attention as this is not the final definition
 */
export interface RegistrationCredentials {
  username?: string;
  email: string;
  password: string;
}
